name: lo
description: "Lỗ - Lỗ nhiều chưa? Gold loss tracker with memes for Vietnamese gold holders"
publish_to: 'none'

version: 0.1.0+2

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # State Management
  provider: ^6.1.2

  # Local Storage
  shared_preferences: ^2.3.2
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # Charts
  fl_chart: ^0.69.2

  # Utilities
  intl: ^0.19.0
  uuid: ^4.5.1

  # Networking + parsing (SjcPriceAdapter)
  http: ^1.2.2
  xml: ^6.5.0

  # UI
  google_fonts: ^6.2.1

  # Ads
  google_mobile_ads: ^9.0.0

  # Push notification
  firebase_core: ^4.11.0
  firebase_messaging: ^16.4.1
  flutter_local_notifications: ^22.0.1

  # Xuất file/chia sẻ (Cài đặt → Xuất CSV/JSON)
  path_provider: ^2.1.5
  share_plus: ^13.2.0

  # Xin đánh giá app (In-App Review)
  in_app_review: ^2.0.12

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  hive_generator: ^2.0.1
  build_runner: ^2.4.13
  flutter_launcher_icons: ^0.14.1

flutter:
  uses-material-design: true
  assets:
    - assets/icon/
    - assets/logo.png
    - assets/author.jpg
    - assets/data/sjc_history.json

# Cấu hình flutter_launcher_icons.
# Chạy: dart run flutter_launcher_icons
flutter_launcher_icons:
  image_path: "assets/logo.png"
  android: "ic_launcher"
  ios: true
  remove_alpha_ios: true
  web:
    generate: true
    image_path: "assets/logo.png"
    background_color: "#121212"
    theme_color: "#FFD700"
  windows:
    generate: false
  macos:
    generate: false
  # Adaptive icon Android 8+ — foreground là logo, background màu đen chủ đạo.
  adaptive_icon_background: "#121212"
  adaptive_icon_foreground: "assets/logo.png"
  min_sdk_android: 21
